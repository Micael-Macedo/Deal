@model IEnumerable<Deal.Models.NotaPrestador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Criar</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Avaliação
            </th>
            <th>
                Prestador
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Avaliacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prestador.Nome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotaPrestadorId">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.NotaPrestadorId">Detalhe</a> |
                <a asp-action="Delete" asp-route-id="@item.NotaPrestadorId">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>

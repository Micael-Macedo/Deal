@model IEnumerable<Deal.Models.NotaCliente>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Avaliacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Avaliacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.ClienteId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotaClienteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NotaClienteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NotaClienteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

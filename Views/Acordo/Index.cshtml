@model IEnumerable<Deal.Models.Acordo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AcordoId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkServico)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AcordoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkServico)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AcordoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AcordoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AcordoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

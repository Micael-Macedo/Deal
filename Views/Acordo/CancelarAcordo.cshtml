@model Deal.Models.Acordo
@{
    ViewData["Title"] = "CancelarAcordo";
}

<h1>@ViewData["Title"]</h1>

<div class="container_delete" style="justify-content: center; align-items: center;">
    <div class="titulo_formulario_index">
        <h1 style="font-weight: 840; font-size: 45px;">Deseja Cancelar o Acordo?</h1>
    </div>
    <div class="WarningInfo">
        <p>Em caso de cancelamento o serviço deixará de existir e sua pontuação é reduzida conforme mais acordos forem cancelados</p>
    </div>
    <hr style="margin: 40px 0 20px;" />
    <div class="formContent">
        <form asp-action="CancelarAcordo" method="post">
            <input type="hidden" asp-for="AcordoId" />
            <div class="form-group">
                <label for="justificativa" class="control-label">Justificativa</label>
                <input type="text" style="width: 400px; height: 120px;" name="justificativa" class="form-control">
            </div>
            <div class="group-cliente-servicos-button" style="margin: 20px 0px 30px 0px;">
                <button type="submit" value="Cancelar" name="Cancelar" class="btn btn-vermelho">Cancelar
                    Acordo</button>
                <a asp-action="AcordoCliente" asp-route-id="@Model.AcordoId" class="btn btn-roxo">Retornar ao
                    Acordo</a>
            </div>
        </form>
    </div>
</div>
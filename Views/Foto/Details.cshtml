@model Deal.Models.Foto

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Foto</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.FotoPortfolio)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FotoPortfolio)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.FotoId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>

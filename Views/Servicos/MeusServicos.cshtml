@model IEnumerable<Deal.Models.Servico>
@{
    ViewData["Title"] = "Meus Serviços";
    var idCliente =  ViewData["IdCliente"]; 
}
<<<<<<< HEAD

<h1>@ViewData["Title"]</h1>
<a asp-action="Home" asp-controller="Clientes" asp-route-id="@idCliente" class="btn btn-roxo-escuro">Retornar a pagina
    inicial</a>
    <a asp-action="Create" asp-controller="Servicos" asp-route-id="@idCliente" class="btn" style="background-color: #35e44c;">Criar Serviço</a>
<div class="AllServicesCards">
=======
    <div class="titulo_formulario_index">
        <h1 style="font-weight: 840; font-size: 45px; display: flex">Seus serviços</h1>
    </div>
>>>>>>> main

    @foreach (var item in Model)
    {   
        <div class="container centralizar">
            <div class="servicos-page">
                <div class="txt-cliente-servicos">
                    <div class="row centralizar">
                        <div class="row" style="width: 900px;">
                        <dt class="col">
                            Servico:
                        </dt>
                        <dd class="col-2">
                            @Html.DisplayFor(modelItem => item.Nome)
                        </dd>
                        <dt class="col-2">
                            Status:
                        </dt>
                        <dd class="col-2">
                            @Html.DisplayFor(modelItem => item.Status)
                        </dd>
                        <dt class="col-2">
                            Categoria:
                        </dt>
                        <dd class="col-2">
                            @Html.DisplayFor(modelItem => item.Categoria.Atuacao)
                        </dd>
                        <dt class="col-2">
                            Descrição:
                        </dt>
                        <dd class="col-5">
                            @Html.DisplayFor(modelItem => item.Descricao)
                        </dd>
                        
                        @if (@item.FkPrestador != null)
                        {
                            <dt class="col-sm-1">
                                Prestador
                            </dt>
                            <dd class="col-sm-10">
                                @Html.DisplayFor(modelItem => item.Prestador.Nome)
                            </dd>
                        }
                    </div>
                    </div>
                <div class="group-cliente-servicos-buttons">
                    @if (item.IsAcordoFeito != true)
                    {
                        <a asp-action="Edit" asp-route-id="@item.ServicoId" class="btn btn-roxo">Editar</a> 
                        <span>|</span>
                    }
                    <a asp-action="Details" asp-route-id="@item.ServicoId" class="btn btn-roxo">Detalhes</a> |
                    @if (item.IsAcordoFeito)
                    {
                        <a asp-action="AcordosCliente" asp-controller="Acordo" asp-route-id="@item.FkCliente" class="btn btn-roxo">Ver Acordos</a>
                        <span>|</span>
                    }
                    @if (item.IsDisponivel == true && item.IsAcordoFeito != true && item.Status != "Convite enviado")
                    {
                        <a asp-action="AddPrestador" asp-route-id="@item.ServicoId" class="btn" style="background-color: #35e44c;">Selecionar Prestador</a>
                        <span>|</span>
                    }
                    @if (item.IsAcordoFeito != true && item.Status != "Convite enviado" && item.Status != "Prestador Recusou o Serviço" && item.FkPrestador != null)
                    {
                        <a asp-action="ConfirmarAcordo" asp-route-id="@item.ServicoId" class="btn" style="background-color: #35e44c;">Confirmar Acordo</a>
                        <span>|</span>
                    }
                    
                    @if (item.IsAcordoFeito != true)
                    {
                        <a asp-action="ClienteCancelaServico" asp-route-id="@item.ServicoId" class="btn btn-vermelho" style="font-weight: 600; margin: 10px;">Cancelar Servico</a>
                    }

                        <a asp-action="Create" asp-controller="Servicos" asp-route-id="@idCliente" class="btn btn-verde"  style="font-weight: 600; margin: 10px;">Criar Serviço</a>
                </div>
                </div>
            </div>
        </div>
    }

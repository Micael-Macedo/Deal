@model Deal.Models.Foto

@{
    ViewData["Title"] = "Editar";
}

<h1>Editar</h1>

<h4>Foto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="FotoId" />
            <div class="form-group">
                <label class="control-label">Foto</label>
                <input asp-for="FotoPrestador" class="form-control" />
                <span asp-validation-for="FotoPrestador" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Portfolio</label>
                <select asp-for="FkPortfolio" class="form-control" asp-items="ViewBag.FkPortfolio"></select>
                <span asp-validation-for="FkPortfolio" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Salvar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Retornar</a>
</div>

